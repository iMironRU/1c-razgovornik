# План глав 5–8

Составлен 30 августа 2026. Читаемая версия — в артефакте, ссылка в диалоге.

## Состояние

31 параграф, все `review`, версия 0.2.0, ошибок линтера нет.

| глава | параграфов | слов |
|---|---|---|
| 0. Предисловие | 3 | 717 |
| 1. Природа языка | 6 | 1 997 |
| 2. Переменные и примитивные типы | 7 | 1 458 |
| 3. Коллекции | 8 | 1 844 |
| 4. Управление потоком | 7 | 2 726 |
| 5–8 | не начаты | — |

Формат параграфа рабочий: проза → запускаемый листинг → таблица «1С / Python /
JavaScript / C#» → «Ловушки при переходе» по направлениям. Четырнадцать параграфов
несут кнопку песочницы.

## Два решения до продолжения

**Размер параграфа.** Спецификация обещает «15–20 минут»; средний параграф — 280 слов,
две минуты. Чтобы выполнить обещание, книгу надо увеличить всемеро. Моё мнение:
обещание неверное, книга правильная — разговорник хорош тем, что справка находится за
полминуты. Правим спецификацию: параграф 2–4 минуты. Решить надо **до** глав 5–8,
иначе вторая половина будет написана в другом масштабе.

**Обязательность таблицы аналогий.** Конституция требует её в каждом параграфе; схему
целиком держат 11 из 31. Таблица есть у 14, ловушки у 18. Иногда это оправданно: в
§ 1.5 сравнивать нечего, у Python нет аналога `&НаСервереБезКонтекста`. Предлагаемая
формулировка: таблица — где аналог есть, ловушки — всегда.

## Мелочи заодно

- 14 ссылок «§ N.N» в тексте — канон серии требует направления вместо адреса.
- «доменных» в § 1.4 — слово из нашего словаря.
- 46 предупреждений о ширине: таблицы на четыре языка не влезают в узкий экран.
  Для этой книги четыре колонки — суть формата; либо мириться, либо отдельно думать
  про мобильную вёрстку.

## Глава 5. Процедуры и функции (7)

Несущая ловушка — передача параметров: в 1С по умолчанию по ссылке, во всех трёх
языках сравнения иначе.

1. Процедура и функция — почему их две
2. Параметры по ссылке и `Знач` — главный параграф главы
3. Значения по умолчанию; нет именованных аргументов и переменного числа
4. `Экспорт` — вся модель видимости
5. Чего нет: перегрузка, лямбды, замыкания; чем заменяют
6. Обработчики событий — сигнатуру задаёт платформа
7. Как прочитать чужую процедуру

## Глава 6. Обработка ошибок (5)

Главное — чего нет: `finally`, типизированный `catch`, свои классы исключений.

1. `Попытка` — `Исключение` — `КонецПопытки`; `finally` нет
2. `ВызватьИсключение`: исключение — строка, а не объект
3. `ИнформацияОбОшибке()` — как разобрать, что случилось
4. Чего нет и чем живут
5. Транзакции и исключения — аналога в трёх языках почти нет

## Глава 7. Асинхронность (5)

Самая обманчивая: `Асинх`/`Ждать` выглядят как `async`/`await`, но решают другую задачу.

1. Откуда взялась: веб-клиент и запрет модальности
2. `ОписаниеОповещения` — колбэки, которые в живом коде повсюду
3. `Асинх` и `Ждать` (8.3.18+) — чем не то же самое
4. Чего нет: промиса как объекта, `Promise.all`, отмены
5. Фоновые задания — вот где параллелизм

## Глава 8. Объекты и модули (6)

Развести с § 1.4, где уже разобраны типы модулей: там «где живёт код», здесь «что такое
объект». Возможно переименовать в «Объекты вместо классов».

1. Классов нет — что вместо
2. Ссылка и объект — аналога нет ни в одном из трёх языков; главный параграф
3. Менеджер — статические методы без класса
4. Что делает `Новый`
5. Структура и Соответствие вместо ad-hoc объектов
6. Как прочитать чужой модуль — итоговый параграф книги

## Порядок работ

1. Два решения выше.
2. Глава 5 — без процедур нельзя прочитать ни один реальный модуль. Параграф 5.2 про
   `Знач` написать первым и обкатать на нём формат.
3. Главы 6 и 8 — встречаются в каждом файле чужого кода.
4. Асинхронность последней: нужна реже, требует сверки по версиям платформы, меньше
   всего мешает читать чужой код.
5. Мелочи — заодно с правкой соответствующих глав.
